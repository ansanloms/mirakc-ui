# dotenv environment variable files (雛形 .env.example は追跡する)
.env
.env.*
!.env.example

# Vite build output
client/dist/
# Vite dev dependency cache
client/.vite/
# TanStack Router generated route tree (vite が生成)
client/routeTree.gen.ts
# test coverage
coverage/
# キーワード自動録画ルールの実行時データ (KEYWORD_RULES_PATH の既定値)
data/
# Storybook 静的ビルド成果物
storybook-static/
# npm dependencies
node_modules/

# just（個人ローカルの上書き）
.just/*.local.just

# design-sync (claude.ai/design 同期) の transient 成果物
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
