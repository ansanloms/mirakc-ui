# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Fresh build directory (移行完了後に削除予定)
_fresh/
# Vite build output
client/dist/
# TanStack Router generated route tree (vite が生成)
client/routeTree.gen.ts
# 移行中の退避ファイル (Phase 5 で削除)
deno.json.old
# npm dependencies
node_modules/

# mise
.mise/config.local.toml
