type: object
description:
  $ref: "./descriptions/AppConfig.md"
required:
  - timeZone
properties:
  timeZone:
    type: string
    description: アプリサーバが日付表示に使う IANA タイムゾーン ID。サーバの TZ 環境変数から解決する。クライアントはこの値で全ての日時表示を整形する。
    example: Asia/Tokyo
