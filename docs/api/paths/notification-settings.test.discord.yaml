post:
  operationId: postNotificationSettingsTestDiscord
  summary: Discord へのテスト通知の送信。
  tags:
    - notification-settings
  description:
    $ref: ./descriptions/postNotificationSettingsTestDiscord.md
  requestBody:
    description: 送信先。保存前の Discord の入力値を受け取る。
    content:
      application/json:
        schema:
          $ref: ../components/schemas/NotificationTestDiscordRequest.yaml
        examples:
          normal:
            value:
              $ref: ../examples/NotificationTestDiscordRequest.yaml
  responses:
    200:
      description: 送信成功時。
      content:
        application/json:
          schema:
            $ref: ../components/schemas/NotificationTestResponse.yaml
          examples:
            succeeded:
              value:
                $ref: ../examples/NotificationTestResponse.yaml
    400:
      description: 入力が不正な場合。
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ResponseError.yaml
          examples:
            invalid:
              value:
                $ref: ../examples/ResponseErrorDiscordTest.yaml
    502:
      description: Discord への送信に失敗した場合。
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ResponseError.yaml
          examples:
            failed:
              value:
                $ref: ../examples/ResponseErrorTestFailed.yaml
